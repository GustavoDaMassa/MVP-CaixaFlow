<% content_for :title, "Editar — #{@customer.name}" %>
<div class="page-header"><h1 class="page-title">Editar Cliente</h1></div>
<div class="card card-body"><%= render "form", customer: @customer %></div>
